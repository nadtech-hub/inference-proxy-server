# Proxy server for Text Embeddings Inference service


In order to run application execute command:
```
docker-compose up --build
```

To stop the application run the command:
```
docker-compose down -v --remove-orphans
```